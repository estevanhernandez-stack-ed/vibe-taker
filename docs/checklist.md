<!-- /checklist output for vibe-taker (Cart cycle #15).
     Generated 2026-05-07 in autonomous mode.
     Inputs: docs/spec.md (primary), docs/prd.md (acceptance criteria source),
     docs/scope.md (cuts to honor), docs/builder-profile.md (build-mode preference),
     process-notes.md (cycle context).
     Sequencing logic: capture is the harder half (build first), bundle schema is
     the contract surface threaded through capture, plant is mostly mechanical once
     schema is locked, list is read-only and lowest risk, end-to-end round-trip on
     the bg-remover gates v1, deployment + docs/security close the cycle. -->

# Build Checklist

## Build Preferences

- **Build mode:** Autonomous — builder is `fully-autonomous` per `~/.claude/profiles/builder.json`. /build executes the whole checklist; checkpoints surface at items 4, 8, and 11.
- **Comprehension checks:** N/A (autonomous mode)
- **Git:** Commit after each item with message format `"Complete step N: <title>"`. Tag is reserved for item 11 (release ritual). No `git push` until item 11 — `main` of `estevanhernandez-stack-ed/vibe-taker` is canary; one push per release tag, not per checklist step.
- **Verification:** On — checkpoints every 3-4 items (after items 4, 8, and 11). At each checkpoint /build pauses with a short artifact summary; builder spot-checks against the spec ref before continuing.
- **Check-in cadence:** N/A (autonomous mode)

## Sequencing Notes

Three load-bearing dependency edges encoded below:

- **Schema before capture body.** Items 2 + 3 (`contract.schema.json`, templates, references) land before item 5/6 (capture skill) so the agent has a typed write target instead of improvising bundle structure. KTD-1 mitigation.
- **Capture before plant.** Plant reads bundles produced by capture. Items 5-6 must produce a real bundle on the shelf before items 7-8 have anything to validate against.
- **Round-trip after both halves.** Item 10 (bg-remover end-to-end) is the v1 gate per scope.md/prd.md; it can't run until 5-9 are in place.

The first 2-3 items have detailed reasoning per the SKILL contract; later items are propose-and-agree shape.

## Checklist

- [ ] **1. Plugin scaffold + manifest + storefront copy**
  Spec ref: `spec.md > Stack` + `spec.md > Plugin Source Layout` + `spec.md > Deployment — Identity & Signing`
  What to build: Create the plugin directory tree at the project root: `.claude-plugin/plugin.json` (manifest with `name`, `version: "0.1.0"`, `description`, `author`, `homepage`, `repository`, `license: "MIT"`, `keywords`), three empty `commands/` files (`capture.md`, `plant.md`, `list.md` — frontmatter only, body filled by their owning items), `skills/` directory tree (`guide/`, `capture/`, `plant/`, `list/`, `session-logger/`, `friction-logger/` — each with an empty `SKILL.md` placeholder so cross-references resolve), `skills/guide/{references,schemas,templates}/` subdirectories, `LICENSE` (MIT), and `README.md` (marketplace storefront copy: anchor, what it does, install command, three commands, link to docs). Match the six-plugin convention exactly (compare against `vibe-cartographer/plugins/vibe-cartographer/` source layout).
  Acceptance: Directory tree matches the spec's Plugin Source Layout block byte-for-byte. `plugin.json` validates against the marketplace.json consumer expectations (parses cleanly, all required fields present). `README.md` reads as a peer to the other six plugins on the storefront — same voice, same sections, no UI screenshots (CLI plugin).
  Verify: `find . -type f | sort` matches the spec's tree diagram. `cat .claude-plugin/plugin.json | python -m json.tool` exits 0. `wc -l README.md` ~ 60-100 lines.

- [ ] **2. Bundle schema — `contract.json` + `index.json` + artifact templates**
  Spec ref: `spec.md > Bundle Schema` + `spec.md > Library Shelf — ~/.vibe-taker/library/` + `prd.md > Epic: Bundle schema`
  What to build: Author the load-bearing schema artifacts. `skills/guide/schemas/contract.schema.json` — JSON Schema covering every required + optional field from the spec's `contract.json` table (schema_version, name, version, language, framework, interface_kind, inputs, outputs, dependencies, env_vars, source_repo, source_path, captured_at, entry_points, tags, summary, notes_completeness). `skills/guide/schemas/index.schema.json` — JSON Schema for the shelf manifest top-level shape (schema_version, bundles[]). Templates: `skills/guide/templates/README.md.template`, `architecture.md.template` (with required Summary / Components / Data flow sections), `notes.md.template` (with required Why-this-exists / Gotchas sections, "None known." sentinel allowed). Reference doc: `skills/guide/references/bundle-schema.md` — readable summary of both schemas + the six-artifact directory layout, cross-linked to spec.md sections. KTD-2: lock at `schema_version: "1.0"`.
  Acceptance: `prd.md > Bundle-schema epic > stories 1-2` acceptance criteria all hold. Schemas validate (`python -c "import jsonschema, json; jsonschema.Draft202012Validator.check_schema(json.load(open(...)))"` passes). Templates compile-render with placeholder-substitution (no bare `{{var}}` markers escape the slot fill). `bundle-schema.md` reads as the single source of truth a Claude Code session in another repo could consume.
  Verify: Hand-write a fake `contract.json` against the schema, run jsonschema validation — should pass. Hand-write a deliberately-malformed one (missing `language`) — should fail with a schema error pointing at the missing field.

- [ ] **3. Shared guide SKILL + reference docs**
  Spec ref: `spec.md > Skill Files > skills/guide/SKILL.md` + `spec.md > Capture Flow Architecture > Secret-file skip` + `spec.md > Plant Flow Architecture > Stack-match decision tree (resolves OQ-3)` + `spec.md > Capture Flow Architecture > Interview gate (resolves heuristic threshold for OQ-5)` + `spec.md > Error / Exit-Code Conventions`
  What to build: `skills/guide/SKILL.md` (internal-only; `user-invocable: false`) with voice/persona handling (defer to `shared.preferences.persona` from `~/.claude/profiles/builder.json`), Tier-1 hygiene rules (output discipline, working-directory discipline, verify-before-synthesizing), and cross-references to the four reference files. Then the four references: `skills/guide/references/secret-patterns.md` (full glob list from spec section 6), `stack-match.md` (the framework-family table verbatim, with the family taxonomy), `interview-gate.md` (≥30 char + Levenshtein < 8 substantive-item heuristic + the four interview questions), `error-contract.md` (three-class exit conventions + the discipline rule that every class-1/class-2 prints a one-line recovery action).
  Acceptance: Every command SKILL (capture, plant, list) can read `guide/SKILL.md` first per the spec's "Reads `skills/guide/SKILL.md` for shared persona / voice / hygiene before doing the command's work" contract. Reference files are addressable from spec.md cross-links — every `references/*.md` reference in spec.md resolves to a real file. Voice baseline matches the marketplace storefront copy (builder-to-builder, second person, sentence case).
  Verify: `grep -r "skills/guide/references" plugins/vibe-taker/` resolves all references. Open `guide/SKILL.md` and read top-to-bottom — should read as a peer to the other plugins' guide skills.

- [ ] **4. Self-Evolving Plugin Framework placeholders**
  Spec ref: `spec.md > Skill Files > skills/session-logger/SKILL.md` + `skills/friction-logger/SKILL.md` + `spec.md > Self-Evolving Plugin Framework Compatibility` + `spec.md > Key Technical Decisions > KTD-8`
  What to build: `skills/session-logger/SKILL.md` documents the contract (`start(command, project_dir) → sessionUUID`, terminal-append at command end with outcome + decisions + artifact path) and the reserved data path `~/.claude/plugins/data/vibe-taker/sessions/<date>.jsonl`. Explicitly states command skills do **not** invoke this in v1 — reserved for v2. `skills/friction-logger/SKILL.md` documents the `log(entry)` contract, the data path `~/.claude/plugins/data/vibe-taker/friction.jsonl`, and references `skills/guide/references/friction-triggers.md` (also placeholder in v1 — produce as a stub stating "Triggers table reserved for v2"). KTD-8: architecturally compatible, not load-bearing.
  Acceptance: `prd.md` doesn't gate on these (Self-Evolving Plugin Framework is explicitly cut from v1 per scope.md). Spec table `## Self-Evolving Plugin Framework Compatibility` rows for Levels 2-3 read "skill placeholder + reserved data path; not invoked." Folder names + data paths exist on disk so v2 lights them up without restructure.
  Verify: ✅ **Checkpoint #1** — pause here, summarize items 1-4 to the builder. `ls plugins/vibe-taker/skills/` shows all six skill folders. No command skill imports session-logger or friction-logger anywhere — verified via `grep -r "session-logger\|friction-logger" plugins/vibe-taker/skills/{capture,plant,list}/` returning empty.

- [ ] **5. Capture skill — target resolution + autonomous read pass + secret-file skip**
  Spec ref: `spec.md > Capture Flow Architecture` sections 1, 2, 6 + `prd.md > Epic: Capture > stories 1-5, 8`
  What to build: `commands/capture.md` (frontmatter with `description` and `argument-hint` per spec; one-paragraph body deferring to the skill). `skills/capture/SKILL.md` first half: read `skills/guide/SKILL.md`, then implement (a) target resolution table (single file / folder / glob / in-file selector decline) with the verbatim decline message from PRD Capture story 4, (b) autonomous read pass — walk every file, language sniff, manifest detect (`package.json`, `pyproject.toml`, `requirements.txt`, `Cargo.toml`, `*.csproj`, `Gemfile`, `go.mod`), entry-point identification by language, I/O extraction (argparse / Typer / Click / yargs / Commander declarations), env-var regex grep, prompt extraction (>100 char strings to recognized LLM SDK calls), intent extraction (READMEs + top-of-file docstrings), gotcha pattern-match against the known-list (sharp, pillow, playwright, tensorflow/torch, aws/gcp/azure), (c) secret-file skip with load-bearing detection. **No bundle written yet** — this item produces in-memory analysis printed to stdout for the builder to inspect.
  Acceptance: `prd.md > Capture > stories 1-5 + 8` acceptance criteria for read/extract behavior hold. Glob no-match exits 1 with "no files matched" error. In-file selector exits 1 with the verbatim message. Skipped secret files print to stdout. Load-bearing detection identifies env-var stubs from imports/requires of skipped basenames. Stdout summary lists: detected language, framework, interface_kind, count of inputs/outputs/env_vars/prompts/gotchas extracted.
  Verify: Run `:capture` against the 626labs hub bg-remover folder (read-only — no shelf write yet). Stdout should print analysis: `language: python`, `framework: cli-argparse`, named entry-point file, ≥1 input + ≥1 output extracted, OPENAI_API_KEY (or equivalent) detected as env var, prompts directory non-empty if the script calls an LLM. Run against a glob that matches nothing — should exit 1 cleanly. Run with `:120-180` line-range syntax — should print the decline message and exit 1.

- [ ] **6. Capture skill — interview gate + bundle generation + atomic write + versioning**
  Spec ref: `spec.md > Capture Flow Architecture` sections 3, 4, 5 + `spec.md > Bundle Schema > Versioning rules (resolves OQ-1)` + `spec.md > Library Shelf — Atomic writes` + `prd.md > Epic: Capture > stories 5-7`
  What to build: Second half of `skills/capture/SKILL.md`: (a) interview gate — fires when notes < 3 substantive items OR no derivable intent OR `prompts/` empty AND deps include LLM SDK OR slug collides with existing index entry. Asks at most 4 questions (intent / shelf-name / tradeoffs / tags). (b) Bundle generation — fill the three artifact templates from autonomous + interview material; build `contract.json` directly from analysis; write extracted prompts verbatim to `prompts/<descriptive-name>.txt`; `cp -R` source into `reference/`. (c) Atomic write pattern — stage at `~/.vibe-taker/library/.staging/<name>-<version>-<unix-timestamp>/`, `mv` to final on success; `index.json.tmp` + `mv` for index updates; clean up `.staging/<*>` older than 1 hour at start of each capture. (d) Versioning detection — read `index.json` at top, detect slug collision, prompt `[y/N]` to bump, move legacy bare-`<name>/` to `<name>/v1/` before writing new bundle to `<name>/v2/` if needed. (e) First-run `~/.vibe-taker/README.md` privacy notice (PRD Library-management story 4). KTD-3 + KTD-5.
  Acceptance: `prd.md > Capture > stories 5-7` + `prd.md > Library-management > story 4` acceptance criteria all hold. Interview fires only when gated; otherwise prints "intent derived autonomously — bundle ready at <path>; edit `notes.md` if anything's missing." Versioning prompt shows on slug collision; `n` exits with no changes; `y` produces `<name>/v<n+1>/` and migrates legacy if needed. All bundle writes go through `.staging/` then `mv`. First-ever capture writes the privacy notice to `~/.vibe-taker/README.md` and stdout.
  Verify: Run `:capture` against the bg-remover. Bundle should land at `~/.vibe-taker/library/bg-remover/v1/` with all six artifacts populated. `cat ~/.vibe-taker/library/index.json | python -m json.tool` shows the new entry. Re-run `:capture` against the same target — versioning prompt should fire; reply `n` and verify no shelf change; reply `y` and verify `v2/` lands plus `index.json.versions[]` grows. Force a sub-3-substantive-notes case (capture a 5-line script with no docs) — interview gate should fire.

- [ ] **7. Plant skill — bundle load + stack detect + decision tree + hard-decline path**
  Spec ref: `spec.md > Plant Flow Architecture` sections 1, 2, 3, 6 + `prd.md > Epic: Plant > stories 2-4`
  What to build: `commands/plant.md` (frontmatter + body deferring to skill). `skills/plant/SKILL.md` first half: read `skills/guide/SKILL.md`, then (a) bundle load — read `index.json`, find by name (case-sensitive exact), resolve `--version=vX` else `latest_version`, read + JSON-Schema-validate `contract.json`, exit 1 with discoverability message ("run `/vibe-taker:list` to see what's available") on bundle-not-found, exit 2 on schema invalid. (b) Stack detect — walk cwd (one level up if needed) for manifests in priority order (`package.json` → Node, `pyproject.toml`/`requirements.txt`/`setup.py` → Python, `Cargo.toml` → Rust, `go.mod` → Go, `*.csproj`/`*.sln` → C#, `Gemfile` → Ruby, `composer.json` → PHP), extract framework from manifest deps. (c) Decision tree — apply the framework-family table from `references/stack-match.md`: high (same lang + same family) → code-lift, low (same lang + different family) → spec-driven, hard (different lang) → decline, no-manifest fallback → spec-driven with notice. (d) Hard-mismatch decline — print the verbatim message from PRD Plant story 3 with bundle paths interpolated, exit 1, **no file written**. KTD-4.
  Acceptance: `prd.md > Plant > stories 2-4` acceptance criteria hold. Bundle-not-found exits 1 with the run-`:list` recovery line. Schema-invalid exits 2 with bundle path + schema error. Hard mismatch produces verbatim decline message and writes nothing. No-manifest target falls back to spec-driven with the printed notice. Decision header includes `Mode: <code-lift | spec-driven>` and `Stack match: <high | low | hard-decline>` (the diff itself comes in item 8).
  Verify: Try `:plant nonexistent-bundle` — should exit 1 with the discoverability message. Hand-corrupt `~/.vibe-taker/library/bg-remover/v1/contract.json` (delete `language` field) — `:plant bg-remover` should exit 2 with the schema error. From a Rust target repo, `:plant bg-remover` (Python source) should print the hard-mismatch decline message verbatim and write zero files. From an empty directory (no manifests), `:plant bg-remover` should print the no-manifest fallback notice and proceed to spec-driven (diff render is item 8).

- [ ] **8. Plant skill — code-lift + spec-driven + diff confirmation + dashboard log**
  Spec ref: `spec.md > Plant Flow Architecture` sections 4, 5, 7, 8 + `prd.md > Epic: Plant > stories 1, 5`
  What to build: Second half of `skills/plant/SKILL.md`: (a) Code-lift mode — copy `<bundle>/reference/` files into target with target-conventional placement (`src/<package>/<feature>/` for Python `pyproject.toml` projects with `src/` layout; `src/<feature>/` for Node with `src/`; `<feature>/` at root otherwise), import rewrite (Python `from .` and `from <pkg>.` paths adjusted to target package name from `[project].name`; Node import paths adjusted to `tsconfig.json#paths` if present, else relative), per-file fallback to spec-driven when no in-language adapter wires the framework call site. (b) Spec-driven mode — read `architecture.md` + `contract.json` + `notes.md`, generate fresh code in target stack matching contract surface (same inputs, outputs, env vars), keep `reference/` as guide. (c) Diff render + single mandatory checkpoint — stage all writes, render unified diff to stdout with the mode/stack-match header from item 7, prompt `[y/N]`, exit 0 with no writes on `n` or empty, atomic per-file `<path>.tmp` + `mv` on `y`. **No `--yes` flag in v1 (KTD-7).** (d) Dashboard decision-log integration — opt-in / fail-silent. After successful plant, check whether `mcp__626Labs__manage_decisions` is in the runtime tool list; if yes, call with action `log` and the body per spec; if no, succeed silently with no error and no retry.
  Acceptance: `prd.md > Plant > stories 1, 5` acceptance criteria hold. **No file is written without explicit `[y/N]` confirmation.** Diff header names mode + stack-match level. Decline at the prompt produces zero writes. Dashboard call fires when MCP is available; absence is silent. Code-lift on same-lang same-family produces import-rewritten files in target-conventional locations. Spec-driven on same-lang different-family produces fresh code matching the contract.
  Verify: ✅ **Checkpoint #2** — pause here, summarize items 5-8 to the builder. `:plant bg-remover` from a Python CLI test repo (high match: Python + cli-argparse) — verify diff renders, decline (`n`) writes nothing, accept (`y`) produces files at `src/<package>/bg_remover/` (or root convention), imports resolve. From a Python web-target (low match: Python + FastAPI) — verify diff names `Mode: spec-driven` and produces fresh code. Check `mcp__626Labs__manage_decisions` log entry shows up in the dashboard if MCP is connected; verify plant succeeds silently when MCP is absent (test by disconnecting or running in a clean shell).

- [ ] **9. List skill — read + search + sort + dedup hint**
  Spec ref: `spec.md > List Flow Architecture` + `prd.md > Epic: Library management > stories 1-3`
  What to build: `commands/list.md` (frontmatter + body deferring to skill). `skills/list/SKILL.md`: read `skills/guide/SKILL.md`, then (a) default print — read `~/.vibe-taker/library/index.json`, validate against `index.schema.json`, exit 0 with empty-library message if absent, exit 2 with corrupt-index recovery if invalid, format each bundle as the three-line block from spec section 1, default sort by `captured_at` of `latest_version` descending. (b) `--search <query>` — case-insensitive substring across name, summary, tags, source_repo, language; empty result prints "no matches" + exit 0. (c) `--sort=name` (alphabetical), `--sort=lang` (group by language then alphabetical). (d) Dedup hint — Jaccard token similarity (lowercase, split on non-alphanumeric, drop stop-words) over `summary` strings; if ≥0.70 between any two bundles, append `[similar to: <other-name>]` to both listings. Best-effort, runs at `:list` time only — never at `:capture` time. Read-only — no writes. KTD: 70% threshold per OQ-2 default; tunable post-ship.
  Acceptance: `prd.md > Library-management > stories 1-3` acceptance criteria hold. Empty library prints the friendly empty-library message + capture hint. Corrupt index exits 2 with backup-recovery line. Sort flags work as specified. Search is case-insensitive across all five fields. Dedup hint flags only at ≥0.70 similarity, both bundles in the pair get the marker. Item ordering is deterministic given the same index.
  Verify: With one bundle on the shelf, `:list` prints one block in spec format. `:list --search bg` matches the bg-remover. `:list --search nonexistent` prints "no matches" and exits 0. Move `index.json` aside — `:list` prints the empty-library message. Corrupt the index (cut last `]`) — `:list` exits 2 with the recovery message. Capture a second bundle with a near-duplicate summary — `:list` should append `[similar to: ...]` to both.

- [ ] **10. End-to-end round-trip — capture + plant on the bg-remover, second-feature smoke test**
  Spec ref: `prd.md > What We're Building` + `scope.md > What "Done" Looks Like`
  What to build: This is the v1 gate item — exercise the full round-trip on the canonical test case, then a second feature for the schema-stability check. (a) From a fresh state (`rm -rf ~/.vibe-taker/library/`), `:capture` the 626labs hub CLI bg-remover script (resolve the actual path to `apps/bg-remove/` or whichever exact path is current in the hub repo). Verify all six artifacts populated, autonomous-derived intent (interview gate did NOT fire — `notes_completeness.substantive_count` ≥ 3), `index.json` clean. (b) From a fresh test repo (a clean Python `pyproject.toml` project, e.g., `mkdir /tmp/vibe-taker-target && cd /tmp/vibe-taker-target && uv init`), `:plant bg-remover`. Verify diff renders, accept it, verify the resulting CLI bg-remover is functional in the target (run it against a sample image). (c) Second-feature smoke test — pick one more feature from a 626labs repo (e.g., a small CLI or agent prompt — builder picks at run-time), `:capture` it, verify the bundle schema doesn't expose a "we need a new field" surprise. (d) Update `process-notes.md > /build` section with what fired the interview gate (or didn't), what the diff actually rendered, and any fields the schema didn't carry that v2 will need.
  Acceptance: All three v1 ship gates from `prd.md > What We're Building` hold: (1) capture works on bg-remover, (2) plant re-installs into a different test repo as a working CLI bg-remover, (3) bundle schema feels stable across the second-feature capture (no missing-field surprise). `scope.md > What "Done" Looks Like` stretch goal (second feature captures cleanly without schema changes) is verified or explicitly logged as friction for `/reflect`.
  Verify: Run the resulting planted bg-remover against a sample image — it should produce a transparent-bg output. Show the bundle directory tree at `~/.vibe-taker/library/bg-remover/v1/` matches the spec layout exactly. Show the second feature's bundle has zero hand-edits required to `contract.json` after capture.

- [ ] **11. Solo repo + GitHub release + marketplace.json ref bump**
  Spec ref: `spec.md > Deployment — Identity & Signing` + `spec.md > Runtime & Deployment` + `builder-profile.md > Deployment Target`
  What to build: Two-repo promotion ritual. (a) Solo repo: `git init` in the project root, create `.gitignore` (covers `.env`, `node_modules/`, `__pycache__/`, `.vibe-taker/` — the user's local shelf must NOT be checked in), initial commit, `gh repo create estevanhernandez-stack-ed/vibe-taker --public --source . --remote origin --push`, then `git tag v0.1.0`, `git push origin v0.1.0`, `gh release create v0.1.0 --title "v0.1.0" --notes <release-notes-from-tag-commits>`. (b) Marketplace ref: in a separate working tree at `C:\Users\estev\Projects\vibe-plugins\` (verify `pwd` per Tier-1 working-directory discipline), edit `.claude-plugin/marketplace.json` to add a `vibe-taker` entry pinned to the `v0.1.0` tag, commit, push. **Never edit both repos in the same session in parallel** — sequence them one at a time per `vibe-plugins/CLAUDE.md` "What NOT to do." (c) If `superpowers:vibe-launch` is installed at `/build` time, defer release-cutting to it; otherwise the agent runs `gh release create` directly.
  Acceptance: Tag `v0.1.0` exists on `estevanhernandez-stack-ed/vibe-taker:main`; GitHub release at that tag is visible. `vibe-plugins/.claude-plugin/marketplace.json` has a `vibe-taker` entry whose ref is `v0.1.0`, committed and pushed. `.gitignore` excludes `~/.vibe-taker/` shelf path equivalents. The two repos were edited in sequence (verifiable from commit timestamps), not in parallel.
  Verify: ✅ **Checkpoint #3** — pause here, summarize items 9-11 to the builder. Visit `https://github.com/estevanhernandez-stack-ed/vibe-taker/releases/tag/v0.1.0` — release exists. From a fresh marketplace install (`/plugin install vibe-taker@vibe-plugins`), `:capture` and `:plant` and `:list` resolve and run. `git log --oneline` in both repos shows non-overlapping commit timestamps.

- [ ] **12. Documentation & security verification**
  Spec ref: `prd.md > What We're Building` + `spec.md > Error / Exit-Code Conventions` + `spec.md > Dependencies & External Services`
  What to build: Final hygiene pass before declaring v1 real. (a) **Documentation:** confirm `README.md` covers what vibe-taker does, install (`/plugin install vibe-taker@vibe-plugins`), the three commands with one-liner syntax + example, the bundle layout, link to `docs/spec.md`. Confirm `docs/{scope,prd,spec,checklist}.md` reflect post-build reality (any spec drift caught during /build is back-merged). Add `docs/changelog.md` with v0.1.0 entry. Add a `.env.example` if any env vars are referenced anywhere in the plugin codebase (they shouldn't be — vibe-taker has no runtime executable — confirm none exist). (b) **Security verification:** run secrets scan — `gh secret-scanning` on the public repo plus `git log --all -p | grep -iE "password|secret|api_key|token"` shows no matches; verify `.gitignore` covers `.env`, `~/.vibe-taker/`, `node_modules/`. (c) **Dependency audit:** vibe-taker has zero runtime dependencies (markdown plugin) — confirm no `package.json`/`requirements.txt`/`Cargo.toml` shipped in the plugin source. (d) **Input validation spot-check:** the only "input" surface is slash-command arguments parsed by the agent — confirm globs/paths are not passed unsanitized into shell commands inside SKILLs (verify with `grep -r "Bash\|exec\|os.system\|subprocess" plugins/vibe-taker/skills/`). (e) **Auth & access control:** N/A — no protected routes, plugin runs locally. (f) **Deployment security posture:** `~/.vibe-taker/` is local-only by default per KTD-6; confirm the privacy notice prints on first capture and lives in `~/.vibe-taker/README.md`. The 626Labs Dashboard MCP integration is opt-in/fail-silent, no secrets in plugin source.
  Acceptance: README clears "someone clones the repo and gets it running" test. `docs/changelog.md` v0.1.0 entry exists. Secrets scan clean. No runtime-deps lockfiles. No unsanitized shell-arg paths in SKILL bodies. Privacy notice present + verified. v1 ship gate from `prd.md > What We're Building` is fully satisfied.
  Verify: Clone the public repo to a fresh location (`gh repo clone estevanhernandez-stack-ed/vibe-taker /tmp/vt-fresh`), follow the README to install via `/plugin install vibe-taker@vibe-plugins`, run `:list` (should show empty-library message on a fresh shelf), then `:capture` against any small folder — should produce a bundle. Run `git log --all -p | grep -iE "password|secret|api_key|token"` on both repos — should return zero matches. Run `grep -r "subprocess\|os.system\|exec(" plugins/vibe-taker/skills/` — should return zero matches outside of explicit `Bash` tool invocations whose arg shapes are documented in the SKILL.

<!-- Sequencing dependencies graph (for /build):
       1 → 2, 3, 4
       2, 3 → 5
       5 → 6
       2, 3, 6 → 7
       7 → 8
       3, 6 → 9
       6, 8, 9 → 10
       10 → 11
       11 → 12
     Items 5-9 are the bulk of new agent reasoning; items 10-12 are validation
     and ship ritual. The three checkpoints (after items 4, 8, 11) match the
     spec's three architectural beats: scaffolding done, round-trip code done,
     deployment done. -->
